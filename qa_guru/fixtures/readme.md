#у нас есть тест:
def test_login():
    pass
#шаги нам сейчас не важны. допустим, этот тест что-то возвращает

#как с этим тестом будут выглядеть фикстуры?
#фикстуры - это такая же функция

#фикстура, которая откроет нам браузер:
def browser():
    pass

#фикстура, возвращающая логопассы:
def user():
    return username, password

#чтобы функции стали фикстурами надо импортировать pytest:
import pytest